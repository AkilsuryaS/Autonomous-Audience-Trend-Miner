# Required. Create a local .env file and replace the placeholder value.
OPENAI_API_KEY=your-openai-api-key

# Optional. Must match the origin serving the React frontend.
FRONTEND_ORIGIN=http://localhost:5173
