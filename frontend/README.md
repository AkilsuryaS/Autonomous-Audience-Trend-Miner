# Audience Trend Miner frontend

Vite + React + TypeScript interface for the Autonomous Audience Trend Miner.
It consumes the FastAPI service configured by `VITE_API_BASE_URL`.

```bash
npm install
npm run dev
```

Use `npm run lint` and `npm run build` for local validation.
